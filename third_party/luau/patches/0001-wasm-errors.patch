diff --git a/Ast/src/Parser.cpp b/Ast/src/Parser.cpp
index 1960cc4..769b23b 100644
--- a/Ast/src/Parser.cpp
+++ b/Ast/src/Parser.cpp
@@ -12,6 +12,13 @@
 #include <string.h>
 #include <unordered_set>
 
+// luauc PATCH: route the Parser's ParseError throw/catch through a trap-backed channel.
+// The Luau guest is built -fno-exceptions (Zig's wasm32-wasi libc++ ships no C++
+// exception runtime). See runtime/include/error_channel.h and the runtime-profile contract for the
+// complete list of upstream edits.
+#include "error_channel.h"
+
 LUAU_FASTINTVARIABLE(LuauRecursionLimit, 1000)
 LUAU_FASTINTVARIABLE(LuauTypeLengthLimit, 1000)
 LUAU_FASTINTVARIABLE(LuauParseErrorLimit, 100)
@@ -41,6 +48,11 @@ bool luau_telemetry_parsed_return_type_variadic_with_type_suffix = false;
 namespace Luau
 {
 
+// luauc PATCH: the single global ParseError trap channel (single-threaded wasm
+// guest). `Parser::parse`/`runParse` open a scope on it; the two
+// `throw ParseError(...)` sites raise on it (see the luauc runtime contract).
+static ::luauc_eh::Channel<ParseError> luaucParserEh;
+
 using AttributeArgumentsValidator = std::function<std::vector<std::pair<Location, std::string>>(Location, const AstArray<AstExpr*>&)>;
 
 struct AttributeEntry
@@ -135,7 +147,7 @@ LUAU_NOINLINE void ParseError::raise(const Location& location, const char* forma
     std::string message = vformat(format, args);
     va_end(args);
 
-    throw ParseError(location, message);
+    luaucParserEh.raise(ParseError(location, message)); // luauc PATCH: was `throw`
 }
 
 ParseErrors::ParseErrors(std::vector<ParseError> errors)
@@ -231,20 +243,25 @@ ParseResult Parser::parse(const char* buffer, size_t bufferSize, AstNameTable& n
 
     Parser p(buffer, bufferSize, names, allocator, options);
 
-    try
-    {
-        AstStatBlock* root = p.parseChunk();
-        size_t lines = p.lexer.current().location.end.line + (bufferSize > 0 && buffer[bufferSize - 1] != '\n');
-
-        return ParseResult{root, lines, std::move(p.hotcomments), std::move(p.parseErrors), std::move(p.commentLocations), std::move(p.cstNodeMap)};
-    }
-    catch (ParseError& err)
+    // luauc PATCH: was `try { … } catch (ParseError& err) { … }`. Run the protected
+    // body under the profile provider's protected boundary (the guest has no C++ EH / longjmp — see
+    // the luauc runtime contract); a ParseError::raise inside traps to here.
+    AstStatBlock* root = nullptr;
+    size_t lines = 0;
+    bool raised = luaucParserEh.run([&] {
+        root = p.parseChunk();
+        lines = p.lexer.current().location.end.line + (bufferSize > 0 && buffer[bufferSize - 1] != '\n');
+    });
+    if (raised)
     {
+        ParseError err = luaucParserEh.take();
         // when catching a fatal error, append it to the list of non-fatal errors and return
         p.parseErrors.push_back(err);
 
         return ParseResult{nullptr, 0, {}, p.parseErrors, {}, std::move(p.cstNodeMap)};
     }
+
+    return ParseResult{root, lines, std::move(p.hotcomments), std::move(p.parseErrors), std::move(p.commentLocations), std::move(p.cstNodeMap)};
 }
 
 template<typename Node, typename F>
@@ -254,10 +271,12 @@ ParseNodeResult<Node> Parser::runParse(const char* buffer, size_t bufferSize, As
 
     Parser p(buffer, bufferSize, names, allocator, options);
 
-    try
-    {
-        Node* expr = f(p);
-        size_t lines = p.lexer.current().location.end.line + (bufferSize > 0 && buffer[bufferSize - 1] != '\n');
+    // luauc PATCH: was `try { … } catch (ParseError& err) { … }` (see the luauc runtime contract).
+    Node* expr = nullptr;
+    size_t lines = 0;
+    bool raised = luaucParserEh.run([&] {
+        expr = f(p);
+        lines = p.lexer.current().location.end.line + (bufferSize > 0 && buffer[bufferSize - 1] != '\n');
 
         Lexeme eof = p.lexer.next();
         if (eof.type != Lexeme::Eof)
@@ -265,18 +284,19 @@ ParseNodeResult<Node> Parser::runParse(const char* buffer, size_t bufferSize, As
             expr = nullptr;
             p.parseErrors.emplace_back(eof.location, "Expected end of file");
         }
-
-        return ParseNodeResult<Node>{
-            expr, lines, std::move(p.hotcomments), std::move(p.parseErrors), std::move(p.commentLocations), std::move(p.cstNodeMap)
-        };
-    }
-    catch (ParseError& err)
+    });
+    if (raised)
     {
+        ParseError err = luaucParserEh.take();
         // when catching a fatal error, append it to the list of non-fatal errors and return
         p.parseErrors.push_back(err);
 
         return ParseNodeResult<Node>{nullptr, 0, {}, p.parseErrors, {}, std::move(p.cstNodeMap)};
     }
+
+    return ParseNodeResult<Node>{
+        expr, lines, std::move(p.hotcomments), std::move(p.parseErrors), std::move(p.commentLocations), std::move(p.cstNodeMap)
+    };
 }
 
 ParseNodeResult<AstExpr> Parser::parseExpr(const char* buffer, size_t bufferSize, AstNameTable& names, Allocator& allocator, ParseOptions options)
@@ -5193,7 +5213,7 @@ void Parser::report(const Location& location, const char* format, va_list args)
 
     // when limited to a single error, behave as if the error recovery is disabled
     if (FInt::LuauParseErrorLimit == 1)
-        throw ParseError(location, message);
+        luaucParserEh.raise(ParseError(location, message)); // luauc PATCH: was `throw`
 
     parseErrors.emplace_back(location, message);
 
diff --git a/Compiler/src/Compiler.cpp b/Compiler/src/Compiler.cpp
index 6aa1193..2c9c430 100644
--- a/Compiler/src/Compiler.cpp
+++ b/Compiler/src/Compiler.cpp
@@ -22,6 +22,11 @@
 
 #include <math.h>
 
+// luauc PATCH: route the Compiler's CompileError/ParseErrors throw/catch through a
+// trap-backed channel (the guest is built -fno-exceptions). See third_party/luau/glue/error_channel.h
+// and the luauc runtime contract.
+#include "error_channel.h"
+
 LUAU_FASTINTVARIABLE(LuauCompileLoopUnrollThreshold, 25)
 LUAU_FASTINTVARIABLE(LuauCompileLoopUnrollThresholdMaxBoost, 300)
 
@@ -44,6 +49,15 @@ LUAU_FASTFLAGVARIABLE(LuauCompileInlineTableFunctions)
 namespace Luau
 {
 
+// luauc PATCH: global trap-backed channels (single-threaded wasm guest). `luaucCompileEh`
+// pairs the `throw CompileError` at CompileError::raise with the catch in
+// `compile()`. `luaucParseErrorsEh` carries the `throw ParseErrors` in the
+// `compileOrThrow(source)` overload — which this embedding never calls (it goes
+// through `compile()`), so that channel has no handler and raising on it traps
+// deterministically. See the luauc runtime contract.
+static ::luauc_eh::Channel<CompileError> luaucCompileEh;
+static ::luauc_eh::Channel<ParseErrors> luaucParseErrorsEh;
+
 using namespace Luau::Compile;
 
 static const uint32_t kMaxRegisterCount = 255;
@@ -98,7 +112,7 @@ LUAU_NOINLINE void CompileError::raise(const Location& location, const char* for
     std::string message = vformat(format, args);
     va_end(args);
 
-    throw CompileError(location, message);
+    luaucCompileEh.raise(CompileError(location, message)); // luauc PATCH: was `throw`
 }
 
 static BytecodeBuilder::StringRef sref(AstName name)
@@ -5212,7 +5226,7 @@ void compileOrThrow(BytecodeBuilder& bytecode, const std::string& source, const
     ParseResult result = Parser::parse(source.c_str(), source.size(), names, allocator, parseOptions);
 
     if (!result.errors.empty())
-        throw ParseErrors(result.errors);
+        luaucParseErrorsEh.raise(ParseErrors(result.errors)); // luauc PATCH: was `throw` (dead in this embedding)
 
     compileOrThrow(bytecode, result, names, options);
 }
@@ -5234,18 +5248,22 @@ std::string compile(const std::string& source, const CompileOptions& options, co
         return BytecodeBuilder::getError(error);
     }
 
-    try
-    {
+    // luauc PATCH: was `try { … } catch (CompileError& e) { … }` (see the luauc runtime contract).
+    std::string bytecode;
+    bool raised = luaucCompileEh.run([&] {
         BytecodeBuilder bcb(encoder);
         compileOrThrow(bcb, result, names, options);
 
-        return bcb.getBytecode();
-    }
-    catch (CompileError& e)
+        bytecode = bcb.getBytecode();
+    });
+    if (raised)
     {
+        CompileError e = luaucCompileEh.take();
         std::string error = format(":%d: %s", e.getLocation().begin.line + 1, e.what());
         return BytecodeBuilder::getError(error);
     }
+
+    return bytecode;
 }
 
 void setCompileConstantNil(CompileConstant* constant)
diff --git a/VM/src/ldo.cpp b/VM/src/ldo.cpp
index 038ada3..49d11c6 100644
--- a/VM/src/ldo.cpp
+++ b/VM/src/ldo.cpp
@@ -8,12 +8,12 @@
 #include "lmem.h"
 #include "lvm.h"
 
-#if LUA_USE_LONGJMP
-#include <setjmp.h>
+// luauc PATCH: the guest has neither a C++ exception runtime nor working
+// setjmp/longjmp. The
+// error-recovery functions below route Lua's protected calls and errors through
+// the profile provider's protected-call shim instead. See the luauc runtime contract.
+#include "trap.h"
 #include <stdlib.h>
-#else
-#include <stdexcept>
-#endif
 
 #include <string.h>
 
@@ -31,140 +31,40 @@ LUAU_FASTFLAG(LuauCustomYieldablePcalls)
 ** =======================================================
 */
 
-#if LUA_USE_LONGJMP
-struct lua_jmpbuf
-{
-    lua_jmpbuf* volatile prev;
-    volatile int status;
-    jmp_buf buf;
-};
-
-// use POSIX versions of setjmp/longjmp if possible: they don't save/restore signal mask and are therefore faster
-#if defined(__linux__) || defined(__APPLE__)
-#define LUAU_SETJMP(buf) _setjmp(buf)
-#define LUAU_LONGJMP(buf, code) _longjmp(buf, code)
-#else
-#define LUAU_SETJMP(buf) setjmp(buf)
-#define LUAU_LONGJMP(buf, code) longjmp(buf, code)
-#endif
-
+// luauc PATCH: replaces BOTH upstream error paths (setjmp/longjmp under
+// LUA_USE_LONGJMP, and the C++ `lua_exception` path) with the profile provider's
+// protected-call shim. The profile's luauc_runtime_v1_protected_call runs `f` under
+// its declared boundary; luaD_throw records the Lua error code and transfers the
+// native stack back to the matching luaD_rawrunprotected and returns the code.
+// The `errorjmp` field (lstate) is left unused — nothing else reads it.
 int luaD_rawrunprotected(lua_State* L, Pfunc f, void* ud)
 {
-    lua_jmpbuf jb;
-    jb.prev = L->global->errorjmp;
-    jb.status = 0;
-    L->global->errorjmp = &jb;
+    struct Ctx
+    {
+        lua_State* L;
+        Pfunc f;
+        void* ud;
+    } ctx{L, f, ud};
 
-    if (LUAU_SETJMP(jb.buf) == 0)
-        f(L, ud);
-
-    L->global->errorjmp = jb.prev;
-    return jb.status;
+    return luauc_runtime_v1_protected_call(
+        [](void* p)
+        {
+            Ctx* c = static_cast<Ctx*>(p);
+            c->f(c->L, c->ud);
+        },
+        &ctx);
 }
 
 l_noret luaD_throw(lua_State* L, int errcode)
 {
-    if (lua_jmpbuf* jb = L->global->errorjmp)
-    {
-        jb->status = errcode;
-        LUAU_LONGJMP(jb->buf, 1);
-    }
-
-    if (L->global->cb.panic)
-        L->global->cb.panic(L, errcode);
-
-    abort();
+    (void)L;
+    // Record the code for the provider, then transfer to the nearest protected boundary.
+    // If there is none (no active luaD_rawrunprotected), the provider terminates the
+    // guest — the wasm equivalent of the original unprotected-error `abort()`.
+    luauc_runtime_v1_raise(errcode);
 }
-#else
-class lua_exception : public std::exception
-{
-public:
-    lua_exception(lua_State* L, int status)
-        : L(L)
-        , status(status)
-    {
-    }
-
-    const char* what() const throw() override
-    {
-        // LUA_ERRRUN passes error object on the stack
-        if (status == LUA_ERRRUN)
-            if (const char* str = lua_tostring(L, -1))
-                return str;
-
-        switch (status)
-        {
-        case LUA_ERRRUN:
-            return "lua_exception: runtime error";
-        case LUA_ERRSYNTAX:
-            return "lua_exception: syntax error";
-        case LUA_ERRMEM:
-            return "lua_exception: " LUA_MEMERRMSG;
-        case LUA_ERRERR:
-            return "lua_exception: " LUA_ERRERRMSG;
-        default:
-            return "lua_exception: unexpected exception status";
-        }
-    }
-
-    int getStatus() const
-    {
-        return status;
-    }
-
-    const lua_State* getThread() const
-    {
-        return L;
-    }
-
-private:
-    lua_State* L;
-    int status;
-};
-
-int luaD_rawrunprotected(lua_State* L, Pfunc f, void* ud)
-{
-    int status = 0;
-
-    try
-    {
-        f(L, ud);
-        return 0;
-    }
-    catch (lua_exception& e)
-    {
-        // It is assumed/required that the exception caught here was thrown from the same Luau state.
-        // If this assert fires, it indicates a lua_exception was not properly caught and propagated
-        // to the exception handler for a different Luau state. Report this issue to the Luau team if
-        // you need more information or assistance resolving this assert.
-        LUAU_ASSERT(e.getThread() == L);
-
-        status = e.getStatus();
-    }
-    catch (std::exception& e)
-    {
-        // Luau will never throw this, but this can catch exceptions that escape from C++ implementations of external functions
-        try
-        {
-            // there's no exception object on stack; let's push the error on stack so that error handling below can proceed
-            luaG_pusherror(L, e.what());
-            status = LUA_ERRRUN;
-        }
-        catch (std::exception&)
-        {
-            // out of memory while allocating error string
-            status = LUA_ERRMEM;
-        }
-    }
-
-    return status;
-}
-
-l_noret luaD_throw(lua_State* L, int errcode)
-{
-    throw lua_exception(L, errcode);
-}
-#endif
 
 // }======================================================
 
